<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Camera capture temp files (MainActivity writes these to the app's cache dir) -->
    <cache-path name="captured_images" path="." />
    <external-cache-path name="external_captured_images" path="." />
</paths>
